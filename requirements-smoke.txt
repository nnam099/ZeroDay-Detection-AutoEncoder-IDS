# Minimal dependency ranges for compile + unittest smoke checks.
# Runtime deployments use the pinned lock-style versions in requirements.txt.
torch>=2.0
numpy>=1.24
pandas>=1.5
scikit-learn>=1.2
matplotlib>=3.7
pyyaml>=6.0
python-dotenv>=1.0
fastapi>=0.110
httpx>=0.26
ruff>=0.8,<0.9
