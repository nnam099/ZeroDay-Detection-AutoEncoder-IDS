# Sample Flow Logs

Small CSV files in this directory are committed for parser, schema and CI tests.
They are not training datasets.

- `cicflowmeter_sample.csv`: representative CICFlowMeter columns.
- `firewall_flow_sample.csv`: generic firewall or NetFlow-like columns.

Use large local datasets under `data/` or another ignored directory.
